这是什么？

这是一个开箱即用的网络爬虫。

给定一份 **config**（内含 URL 列表路径、输出目录、爬虫参数），即可初始化状态、爬取 HTML、断点续跑。

我们已经处理了各种复杂的情况。是工业可用的？（不确定。要确认一下）




爬虫使用方式：

**依赖（异步爬虫）**：需要 `aiohttp`、`requests`。推荐在本子目录建虚拟环境：

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
```

从仓库根目录 `python main.py` 跑 Paradox 流水线时，总控会自动使用上述 `.venv/bin/python3`（若已安装依赖）。
此时 **不传 config**，且 config 里没有 ``io`` 块时，读写路径由父仓库 ``cfg/base.json`` 的 ``data-folder`` 决定。

**独立任务（推荐）**：只传 config 路径，``io`` 里写明 ``urls_path`` 与 ``output_dir``：

```bash
cd resumable-batch-fetch
python src/main.py cfg/config.json
python src/main.py --config /path/to/job.json
python -c "from pathlib import Path; import sys; sys.path.insert(0,'src'); from main import run_with_config; raise SystemExit(run_with_config('cfg/config.json'))"
```

config 中 ``io`` 字段（相对路径均相对于 **config 文件所在目录**）：

| 字段 | 必填 | 说明 |
|------|------|------|
| ``urls_path`` | 是 | URL 列表 JSON 路径（可与 ``output_dir`` 不在同一目录） |
| ``output_dir`` | 是 | 输出目录（``html/``、sqlite、log） |
| ``sqlite_file`` | 否 | 状态库文件名，默认 ``mod_fetch.sqlite`` |
| ``log_file`` | 否 | 日志文件名，默认 ``output.log`` |

输出目录：允许空目录；允许已有 sqlite（续跑）；**无 sqlite 但目录内已有其它文件** 时报错退出。

示例目录见 ``data_example/`` 与 ``cfg/config.json``。




对于源代码：

1. 编写 job config（可复制 ``cfg/config.json``）。
2. （建议）``python test/checkport.py --config cfg/config.json`` 测试端口。
3. 一步完成初始化 + 爬取：``python src/main.py --config <job.json>``。

或分步：

- ``python src/init_mod_fetch_sqlite.py --config <job.json>`` — 仅从 ``io.urls_path`` 建库（sqlite 已存在则拒绝覆盖）。
- ``python src/fetch_via_id.py --config <job.json>`` — 仅爬取（需已有 sqlite）。

崩溃后再次执行 ``main.py`` 同一 config 即可续跑。




补充：

1. ``python test/stat_mod_fetch.py --config <job.json>`` 查看进度。
2. ``crawler`` / ``safety`` / ``egress_health`` / ``test_url`` 与 ``io`` 写在同一份 config 里。




我们的项目的亮点：

1. 开箱即用、使用起来非常简单。
2. 将配置与运行数据分离（一份 config 描述一次爬取任务）。
