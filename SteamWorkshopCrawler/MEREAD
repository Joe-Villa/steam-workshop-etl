以下是给我自己看的，正式发布时会删除。
cfg：各种配置数据。
help文件夹：所有重要的辅助程序。例如启动多个clash实例，在不同端口分别接入不同代理节点。
status：记录程序运行时的状态
test：测试文件。测试各个状态。



接下来需要做的：
让26561端口（steam++）能够正常运行爬虫。



我们直接使用sqlite来储存



我们要建立一个sqlite表格，来保证数据的一致性。
只有id，status，retry_count三个字段
其中status只有pending,success,fail,invalid四个字段。

逻辑：

1.若没有已有的sql表格，则我们建立一个表，所有的status为pending，所有的retry_count为0。ID从SteamWorkshopCrawler/status/id_collection_state.json当中读取

2.若已有，根据表格内容判断当前爬取情况状态。
初始状态：所有数据的status为pending
状态0：至少一个数据的status为pending
状态1：没有数据的status为pending，且至少一个数据的retry_count为0
状态2：前半句同上，且至少一个数据的retry_count为1
状态3：前半句同上，且至少一个数据的retry_count为2
以此类推。
终止状态：前半句同上，且所有数据的retry_count大于等于我们cfg设置的max_fail

说明：各个状态的含义是：
状态0：全量爬取阶段；状态1：第一轮查漏补缺；状态2：第二轮查漏补缺。以此类推。

当在任意一个阶段找不到可用的id进行爬取时，将自动进入到下一个阶段。

程序运行的逻辑：
根据我们确定的程序状态，做不同的事情：


全量爬取阶段：
寻找一个pending类型的，尝试爬取：
如果成功，必须要成功写入html页面到磁盘，然后将状态设置为success
如果是因为页面不存在，那么状态设置为invalid
如果因为其他原因失败，设置为fail
重复，直到所有的状态变为success,fail或者invalid


第一次查漏补缺：
找到所有fail为0次的，尝试爬取。
对于继续fail的，fail次数加一；不存在或者成功的处理方式和第一次循环一样。

完成：
其中当第n次查漏补缺完成时，终止。（n为我们设定的max_fail次数）


完成后的输出：
我们输出所有fail太多次、或被标记为invalid的模组ID。宣告我们这个步骤圆满完成。其中差的部分应当由人类核对。

















发布之前需要做的：
1.查看是否有中文的注释或者print或者其他输出。有的话，就改为英文。
2.我们给steam++取消了证书校验。记得要对所有用到了26561的地方做修改，确保安全性。
目前有两个地方：crawler单独给26561端口开了，以及checkpoint和checkidcopy。
3.对于vic3analysis/01_collect_mod/00_collect_mod_ids.py，我们也遇到了问题：那就是我们手动取消了证书校验！


需要改的英语语法错误：
1.注意标点符号后面应该有一个空格。
2.检查条件A是否成立。如果成立，则怎么样。怎么说？
#Print game name if it is a valid APPID,if it is, tell user whether it has workshop
3.检查“游戏是否有创意工坊”，应该写成Steam Workshop support 或 lists Steam Workshop on the store
4.井号应该和正文之间加上空格。
5.主语为ports，用which ports are valid 或 whether each port is valid 更自然。
6.steam应该首字母大写。
7.什么是串句？
“If received correct response, means valid” 
应该改为：
We hit https://store.steampowered.com/ through each path in parallel; a port counts as valid if the response is HTTP 200.
8.标点符号之后加上空格。（重复）
9.不是“simultaneously through all ports ”，应该改为每个端口各测一次。更贴切：in parallel, one request per configured port。
10.dilemma：两难选择