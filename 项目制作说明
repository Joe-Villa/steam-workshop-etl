咱们要讲自己是怎么用vibe coding的。刚好，这个项目就是我vibe coding出来的。
项目本身的内容很简单。就是爬取数据，然后做简单的统计表与分析。我重点讲一下我的项目过程。



起因：
就是有一位老哥想要知道victoria 3的创意工坊的情况。刚好，我也想了解，而且感觉应该可以做，所以就立项了。
实际上当时我毛都不会。但我觉得不会可以学，就开干了。




阶段1，能跑就行：
我们首先要定义，我们究竟需要什么？
我看了一下，vic3在创意工坊也就9000多个模组，全部爬了也可以。就决定全量爬取了。先将模组详细页面爬下来，之后再处理。
我发现一个模组有一个唯一的modid，而知道modid，就能够推断出url。
因此，当时的思路就是：收集modid，爬取页面，然后开始分析。
怎么收集APPID呢？我发现在创意工坊主页面往下翻页，能够翻到头。那就这么做了。
翻页，收集APPID，然后开始爬。



当时我就有了流水线的意识了。一个脚本，读取一个输入、给出一个输出。
但脚本都是仅独立运行的，没有组装成一个完整的流水线。
反正各个步骤都非常清晰，按照各个步骤来分别运行，手动操作流水线，就够了。
先收集模组ID，然后爬各个页面，然后分析处理。看起来很好是吧？然而我在爬取各个页面时，就直接遇到了第一个问题：断点重连。





对的。”断点续跑“的思想，在项目开始我就有了。
无他。因为当时的爬虫实在是太不稳定了，随时可能崩溃；而且因为有单IP速率限制，动不动429，运行一次需要花费好几天才能爬完，而我的电脑肯定不能永远开机运行爬虫。因此，我们必须要在爬虫随时崩溃时，能够记住工作进度，下次开始运行时能自动跳过已经完成的任务。
当时我使用的方法就是，每次抓取一个页面，就删除掉modid当中相应的项。



这的确是非常愚蠢的。因为json每次都要写全量，抓一个页面就要写几千行。
不过至少能跑了。我就断断续续跑了若干时间，终于把所有模组的html页面给抓取完了！
对于爬取后的建立表格，我当时就建立了五列：
模组ID，模组标题，模组曝光量、订阅量、收藏量。
也是很简陋。因为我也不知道要做什么统计数据，对面的老哥也不知道要做什么统计数据。
我就随便根据订阅量生成了一些常用的统计数据，就交给老哥了。







阶段2,模组简略页面独立成项目：
然后我突然发现，我们在收集modid的时候，并不是仅仅拿到了一个modid，是先拿到了一个创意工坊简略页的html，然后解析出modid。

这就是vibe coding需要注意的点：如果我们不看，那根本不知道AI是用什么方式实现的。这个我们得注意。它在之后还狠狠坑了我一次。


由于我们本质上爬的是html，而html当中有很多丰富的内容。那么直接丢掉就很可惜了。一定要保存起来。
因为爬虫爬页面很慢。那么爬到的html就很珍贵，应当保存起来而不是丢弃。万一之后用到了呢？
这是个很重要的思想。把贵的重资产保存，而不是删除。因此我爬取到的任何页面，都会找个地方存起来。



因为简略页的html本身就有足够的信息，而且steam的模组简略页根本没有速率限制，只有模组详情页才有。（这是很重要的发现。对同一个网站的不同部分，反爬可能不一样）



因此我们完全可以将“收集modid”转变为一个更有意义的项目：收集所有模组的简略信息。
先将页面爬下来，然后进行处理转换为sqlite，并转换为excel。
字段很详细。而且目前还在用。






阶段3,爬虫独立化：
此时老哥的胃口越来越大。不满足于维多利亚3的9000多个模组数据，居然想要城市天际线，总共30万个模组数据！
我的个老天爷！爬9000个数据就花了好几天，30万个那还能做到吗？看来是必须改革爬虫了。


当时我发现主要的瓶颈在于，爬虫爬数据太慢了。本地处理数据都是几秒钟就做完了，但爬虫爬取详情页面，居然需要运行几十个小时！
因此，我打算将爬虫变为一个独立的模块，独立进行开发与运行。
我发现了我使用的爬虫的共性：本质上就是爬取静态页面的html。


那么我就成功做了一个抽象：给定一个url数组，将其对应的html页面爬下来。
之前的爬取模组详情页，是很愚蠢的：先在上一步给定modid，然后将modid组装成url，然后再爬。这样耦合性就太高了，因为爬虫需要知道怎么将modid转换为url。
我们改了之后，那么只需要外部程序将目标url数组总结好，然后再作为输入即可。我们的爬虫程序不需要知道url是什么，做到了很好的解耦合。



关于爬虫加速，由于主要瓶颈在于单IP访问速率上限，因此思路就是多开几个IP就行了。
将代理IP节点通过程序，直接绑定在端口，是最自然的做法。因此我做了个config，添加所有可用的代理端口，然后开始爬取。
我的思路就是搞一个端口类，每个端口类维护自己的代理节点以及冷却时间。这样一来，就可以不共享主线程的冷却时间了。



架构写好了，但巧妇难为无米之炊，我得真的有代理节点才行。
我之前在站大爷花了50巨款买了代理IP，结果发现不能用，因为steam创意工坊在境外环境，站大爷只有境内IP。



屡次失败后，我最终搞了138个便宜的代理节点。成功开了138个代理端口。加上本来就有的一个，总共139个。
那么爬虫速率到了原来的139倍吗？答案显然是否定的。

第一个难点就是爬虫本身的架构。爬虫本身下载页面，就要花费大约一秒钟；之前5秒/页面的时候还没有感觉，但现在立马就成了最大的瓶颈。
然后我将爬虫从同步给改成了异步。这下总算不阻塞了。




第二个难点在于它们非常不稳定，能不能用都不确定，是不是独立IP也不确定。
次要原因是，我当时理解错了一个参数的意思。我以为是“每次爬取成功后端口冷却时间”，结果实际是“每次爬取成功后主线程的冷却时间”。直接给上限定死了。这就是vibe coding没检查的潜在后果。我花了三天才找到原因。
但毕竟是代理节点，肯定是有帮助的。
我在各个节点的帮助下，吞吐量稳定在了2页/秒，而之前的速度是0.2页/秒，效率提升了大约十倍。
对于30万页的目标，也不是不能爬了。


当然，我知道这只是一个权宜之计。因为节点都非常不干净。
之后要爬取更大规模的数据，肯定还是要用更好的代理节点。不过现在至少能凑合着用。


在折腾代理节点的时候，其他事情我也没有闲着。
例如，对于爬虫状态，我将原来用json，变成了使用sqlite，然后用pending,fail,success,invalid这些状态，来追踪。这样修改起来就非常快，不需要每次都写几千行json了。



阶段4,模块化：
将两个主要内容给模块化了。那么接下来的内容能不能也模块化呢？
答案是肯定的。
我将第三步：将html页面变为统计表并给出统计报告。也变成了一个模块。
这样一来，我们整个程序就变成了三个模块，所有程序逻辑都在模块当中实现。
而除了各个模块以外，我们什么都没有。看起来就像是各个包机械地放在了一起，而没有有机的结合。




阶段5,配置化，主main函数：

将配置文件单独写在一个地方，实际上第一个阶段我做了。但是因为盲目让所有参数配置化，导致cfg又臭又长，根本不想看一眼。
而我在管理各个模块的行为时，发现配置很有用，可以非常方便控制程序行为。
比方说，当时steam++的，tls校验总是出问题，我就在配置当中写了一个ignore_tls_verify，想开就开，想关就关。
再结合之前写爬虫的时候，在cfg当中写了各个ports，我们的cfg越来越完善了。
于是我就有了一个方法：一但有可变的参数，我们全部写在各个cfg当中。
这就让我明白了一个道理：对于cfg怎么写，必须要由人来决定。因为cfg本质上就是给人调整的，我们当然要知道我们需要的cfg有什么。


当时，对于每个包，究竟是独立的项目，还是整体项目的一部分，我根本还说不清楚。
一方面，各个包的独立性非常强，只依赖于数据传递。但另一方面，我们希望能做一个开箱即用的流水线，一个main就能从头到尾将程序给跑起来。
用main将所有程序给串起来，最大的问题就在于data的输出非常混乱。
每个包都有data、input、output。我要跨包来运行，那就就要移动文件；然而各个文件动辄就几个G，根本没法移动。
于是我就做了一个配置：将各个input和output，统一约定在主程序的data文件夹当中。
这的确不是一个好主意，因为我们写死了输入输出路径，各个包的独立性减弱了。我后来还发现了更好的方式。不过至少目前来看，我们有了一个main函数，可以一键运行了。





阶段6，模块化、命令行、统一IO规范、显式编排。
关于各个混乱的包，每个包有不同输入输出路径、不同的配置与参数，实在是太麻烦了。
终于，我想到一个好办法：那就是归整每个包，只暴露最简单的命令行。


怎么做呢？
我们经过总结，发现各个包可以做这样一个抽象：
1.有一个main函数
2.接受若干输入
3.输出若干输出
4.有若干配置文件

那么我们就可以做一个抽象：
一个step，需要传入程序所在位置，以及输入输出、配置文件的所在路径。
我们只传递路径，而不是复制，对于大文件来说非常友好。
而且这给了我们极大的自由度。我们只需要修改pipeline当中的任务列表，就可以成功来进行
有些东西，例如“跳过第二步，只做第一步和第三步”，现在只需要改一下pipeline即可。


最重要的是，现在各个包可以在机器的任何地方，只要暴露一个main函数，接收一个paths路径，就可以了。我们成功做到了统一规范。





7.数据统计解耦
我们现在实现了三个包：一个收集所有模组的简略信息，一个无状态爬虫（主要用于爬取详细页面信息），一个数据统计与分析的包。
我接下来想，数据统计和分析能不能分开呢？也即是说一个包来做ETL的T和L，另一个来专门做下游的分析。
这的确是可行的。我们将数据处理和数据分析给拆了出来，一个包专门整理数据，另一个表专门对数据做统计分析。



8.将流水线配置与检查作为一个独立项目（未来待做）




我对vibe coding的感受：
1.在我看来，程序设计就是有人打方向盘、有人踩油门。AI可以负责踩油门，但是打方向盘，也就是拍板，还是得我自己来干。我觉得自己好像在当赛博老板。我不负责具体的实现，但是负责管理整个项目的方向。说实话，我是比较喜欢AI的发展的。因为这可以让我不用关注技术细节，而将所有精力放在更高层次的事情上。

2.如果你确定你只要用程序做这一件事情，不需要扩展或者用在别的任务，那么就能随便写；但如果你希望你的程序能够用在多个相似的任务，那就应该考虑架构问题了，也应该搞懂程序是怎么实现的了。


3.代码本身不值钱，你做程序设计的思想才值钱。
4.在AI时代，最值钱的不再是具体实现细节，而是变成了领域知识，是“我知道可以往什么方向走”。指望只说一个大致方向，AI就能做出来一个特别好的成果，是不可能的。我们要知道“好的东西”应该长什么样，才能让AI做出来。
5.我现在算是理解了，在代码当中，真正的核心逻辑只有10%，剩下的全都是处理各种异常。我现在每次编写任务，都在想“要是出现了各种乱七八糟的，能不能接住”。这就是程序健壮性，是“能跑”到“工业使用”的区别。

6.在程序设计当中，我们需要做很多拍板。例如“究竟用多线程还是用异步”。这个肯定是需要人来拍板，不能给机器做。
7.显式报告缺失，要求强行用户强制执行默认设定，好过静默执行默认设定。因为用户应当知道程序实际上在做什么。

8.程序的可控性，和使用的方便性，是一个重要的矛盾与平衡。一方面，用户希望用起来尽可能方便，那么知道的要尽可能少；另一个方面，用户希望能对程序有比较精细的控制，那么知道的又要尽可能多。之后我可能还要做分级菜单，但这是之后的事情了。

