# steam-mod-analysis

从 `concrete_info/name.sqlite` 读取建库结果，生成统计报告与图表。

输出目录：`data/<APPID>/report/`（与 `simple_info/`、`concrete_html/`、`concrete_info/` 平级）。

## 用法

```bash
python3 main.py pipeline/run_analysis.json
python3 main.py pipeline/run_analysis.json --game vic3
```

## 目录

| 路径 | 说明 |
|------|------|
| `main.py` | 运行全部分析步骤 |
| `generate_domestic_reports.py` 等 | 各分析脚本（可被 main 调用或单独运行） |
| `cfg/` | 阈值、修复脚本参数 |
| `tool/` | 作者分类、HTML 修复等辅助工具 |

建库见独立子包 **`steam-grant-table/`**。

路径常量见 `data_paths.py`（内部使用 `lib/paradox_paths.py`）。
